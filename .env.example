# CAD Agent — secrets template.
#   cp .env.example .env   then fill in the keys you actually use.
# Loaded at startup by backend/main.py (python-dotenv). Env vars override the
# api_key fields in backend/config/llm_config.yaml.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DASHSCOPE_API_KEY=
DEEPSEEK_API_KEY=
ZHIPU_API_KEY=
